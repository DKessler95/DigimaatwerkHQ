services:
  - type: web
    name: digimaatwerk
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: digimaatwerk-db
          property: connectionString
      - key: EMAIL_PASSWORD
        sync: false

  - type: pserv
    name: digimaatwerk-db
    env: postgresql
    plan: free
    databaseName: digimaatwerk-db
    databaseUser: digimaatwerkdb_user